---
# =============================================================================
# TELEGRAM NOTIFICATION CONFIGURATION
# =============================================================================
# Secrets belong in .env (see .env.example): TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
# Values here are optional fallbacks if env vars are unset.

telegram:
  enabled: true

  # Fallback only — prefer TELEGRAM_BOT_TOKEN in .env
  bot_token: ""

  # Fallback only — prefer TELEGRAM_CHAT_ID in .env
  chat_id: ""

# -----------------------------------------------------------------------------
# NOTIFICATION SETTINGS
# -----------------------------------------------------------------------------
notifications:
  bot_start: true
  bot_stop: true
  trade_entry: true
  trade_exit: true
  running_pnl:
    enabled: true
    interval_seconds: 3600
  # Periodic market snapshot (price, ST, EMA, ADX, position) while bot is running
  market_status_hourly: true
  market_status_interval_seconds: 3600  # 1 hour
  # Alert when SuperTrend flips on a completed primary bar
  supertrend_flip: true
  connection_status: true
  errors: true
